# คู่มือการติดตั้งและ Deploy ระบบแบบละเอียด (ฉบับสมบูรณ์)

เอกสารฉบับนี้จัดทำขึ้นเพื่อให้ทำตามได้ทีละขั้นตอน (Step-by-step) เหมาะสำหรับผู้เริ่มต้น โดยละเอียดทุกจุดเพื่อให้แน่ใจว่าระบบจะออนไลน์ใช้งานได้จริง

---

## 📋 สารบัญ
1.  [เตรียมความพร้อม (Prerequisites)](#ส่วนที่-1-เตรียมความพร้อม)
2.  [เตรียมฐานข้อมูล MySQL (Database)](#ส่วนที่-2-เตรียมฐานข้อมูล-mysql)
3.  [การ Deploy Backend ขึ้น Render](#ส่วนที่-3-การ-deploy-backend-ขึ้น-render)
4.  [การ Deploy Frontend ขึ้น Firebase Hosting](#ส่วนที่-4-การ-deploy-frontend-ขึ้น-firebase-hosting)
5.  [การทดสอบและแก้ไขปัญหา (Troubleshooting)](#ส่วนที่-5-การทดสอบและแก้ไขปัญหา)

---

## ส่วนที่ 1: เตรียมความพร้อม

ก่อนเริ่มขั้นตอน ต้องมั่นใจว่ามีสิ่งเหล่านี้ครบถ้วน:

### 1.1 โปรแกรมที่ต้องมีในเครื่อง
*   **VS Code:** ใช้แก้ไขโค้ด 
*   **Node.js:**  ใช้รันคำสั่ง npm
*   **Git:** ใช้สำหรับส่งโค้ดขึ้น GitHub

### 1.2 บัญชีที่ต้องสมัคร (ฟรีทั้งหมด)
1.  **GitHub Account:** สำหรับเก็บ Source Code (มีแล้ว)
2.  **Render Account (render.com):** สมัครไว้สำหรับรัน Backend Server
3.  **Firebase Account (firebase.google.com):** ใช้อีเมล Google ปกติ สำหรับรัน Frontend
4.  **Clever Cloud (clever-cloud.com) หรือ TiDB Cloud:** สำหรับสร้างฐานข้อมูล MySQL ออนไลน์ (เพราะ Render ไม่มี MySQL ฟรี หรือมีแต่แบบทดลอง 30 วัน ถ้าจะใช้ยาวๆ แนะนำ Clever Cloud หรือ TiDB)

---

## ส่วนที่ 2: เตรียมฐานข้อมูล MySQL

เนื่องจาก Backend ใช้ MySQL เราต้องสร้างฐานข้อมูลออนไลน์ เพื่อให้ Render เชื่อมต่อได้ (จะใช้ localhost ไม่ได้เมื่ออยู่บน Cloud)

**แนะนำ: ใช้ TiDB Cloud หรือ Clever Cloud (ฟรี)**
*สมมติว่าใช้ TiDB Cloud (สมัครง่ายสุด)*

1.  สมัคร [TiDB Cloud](https://tidbcloud.com/)
2.  กด **Create Cluster** เลือก **Serverless (Free)**
3.  เมื่อสร้างเสร็จ กด **Connect**
4.  เลือกว่าจะ Connect ด้วย **General** หรือ **Node.js**
5.  จดค่าเหล่านี้ไว้ใน Notepad:
    *   **Host:** (เช่น `gateway01.us-west-2.prod.aws.tidbcloud.com`)
    *   **Port:** `4000`
    *   **User:** (เช่น `2Pl...xx.root`)
    *   **Password:** (กด Generate Password แล้วจดไว้)
    *   **Database Name:** ตั้งชื่อว่า `nongmim` (อาจต้องเข้าไปสร้าง database ใน UI ของ TiDB หรือใช้คำสั่ง SQL)

> **วิธีรัน SQL สคริปต์สร้างตาราง:**
> ให้ใช้โปรแกรมอย่าง DBeaver หรือ MySQL Workbench ในเครื่อง เชื่อมต่อด้วยค่าข้างบน แล้วรันไฟล์ `nongmim_kindergarten.sql` เพื่อสร้างตารางทั้งหมดเตรียมไว้

---

## ส่วนที่ 3: การ Deploy Backend ขึ้น Render

### 3.1 เตรียม Code บน GitHub
1.  เปิด VS Code
2.  ตรวจสอบไฟล์ `.gitignore` ในโฟลเดอร์ `Backend` ต้อง**ไม่มี** `node_modules` และ `.env` (บรรทัดเหล่านี้ต้องมีอยู่เพื่อความปลอดภัย)
3.  เปิดไฟล์ `Backend/package.json` ตรวจสอบบรรทัด `scripts` ต้องมี:
    ```json
    "start": "node server.js"
    ```
4.  ทำการ Push โค้ดทั้งหมดขึ้น GitHub (commit ล่าสุด)

### 3.2 สร้าง Web Service บน Render
1.  ไปที่ [Dashboard Render](https://dashboard.render.com/) และ Login
2.  กดปุ่ม **New +** สีน้ำเงิน เลือก **Web Service**
3.  เลือก **Build and deploy from a Git repository** กด Next
4.  กด **Connect** ที่ repository `pjnongmim` ของเรา
5.  ตั้งค่าหน้า "Create a new Web Service":
    *   **Name:** `nongmim-backend` (หรือชื่ออื่นตามชอบ)
    *   **Region:** `Singapore (Southeast Asia)` (สำคัญ: เพื่อให้เว็บโหลดเร็วที่สุดในไทย)
    *   **Branch:** `main`
    *   **Root Directory:** `Backend` (**สำคัญมาก:** ต้องใส่คำว่า `Backend` เพราะไฟล์ `server.js` เราอยู่ในนั้น ถ้าไม่ใส่จะ Start ไม่ติด)
    *   **Runtime:** `Node`
    *   **Build Command:** `npm install` (ค่าเริ่มต้น)
    *   **Start Command:** `node server.js` (ค่าเริ่มต้น หรือแก้ให้ตรง)
    *   **Instance Type:** เลือก `Free`

### 3.3 ตั้งค่า Environment Variables (ตัวแปรลับ)
ในหน้านั้น เลื่อนลงมาหาหัวข้อ **Environment Variables** กด **Add Environment Variable** ทีละคู่นะครับ:

| Key (ชื่อตัวแปร) | Value (ค่าที่ต้องใส่) |
| :--- | :--- |
| `NODE_VERSION` | `20.11.0` (หรือเวอร์ชั่นที่เครื่องเราใช้ เพื่อความชัวร์) |
| `DB_HOST` | Host ของฐานข้อมูลออนไลน์ที่ได้จากส่วนที่ 2 |
| `DB_PORT` | Port ฐานข้อมูล (ปกติ MySQL คือ 3306, TiDB คือ 4000) |
| `DB_USER` | User จากส่วนที่ 2 |
| `DB_PASSWORD` | Password จากส่วนที่ 2 |
| `DB_NAME` | `nongmim` |
| `JWT_SECRET` | ตั้งรหัสลับมั่วๆ ยาวๆ เช่น `MySuperSecretKey2026!!` |
| `FIREBASE_SERVICE_ACCOUNT` | **วิธีทำ:** <br>1. เปิดไฟล์ `Firebase/nongmax-web-firebase-adminsdk....json` ใน VS Code <br>2. กด Ctrl+A (เลือกทั้งหมด) แล้ว Ctrl+C (ก๊อปปี้) <br>3. นำมาวางลงในช่อง Value นี้ **ทั้งก้อน** (ห้ามตกหล่นแม้แต่วงเล็บเดียว) |
| `FRONTEND_URL` | ใส่ `https://nongmax-web.web.app` (อันนี้คือ URL ที่เรา *จะ* ได้จาก Firebase เดี๋ยวมาแก้ทีหลังได้ถ้ายังไม่รู้) |

### 3.4 กด Deploy
1.  ตรวจสอบความถูกต้อง แล้วกดปุ่ม **Create Web Service** ด้านล่างสุด
2.  Render จะพาไปหน้า Logs รอประมาณ 3-5 นาที
3.  สังเกตบรรทัดสุดท้ายใน Logs ถ้าขึ้นว่า:
    > `Server running on port 10000` (หรือพอร์ตอื่น)
    > `Your service is live`
    แปลว่า **Backend (Server) ทำงานสำเร็จแล้ว!** ✅
4.  ให้ Copy URL ด้านบนซ้ายของหน้า Render ไว้ (เช่น `https://nongmim-backend.onrender.com`) เราต้องใช้ในขั้นตอนต่อไป

---

## ส่วนที่ 4: การ Deploy Frontend ขึ้น Firebase Hosting

### 4.1 ติดตั้ง Firebase Tool
ที่ VS Code Terminal (ต้องอยู่ที่ root folder `d:\pjnongmim`):
```powershell
npm install -g firebase-tools
```

### 4.2 Login
```powershell
firebase login
```
*   ถ้าขึ้นถาม `Allow Firebase to collect CLI usage...` ตอบ `n`
*   Browser จะเด้งขึ้นมา ให้ Login ด้วย Google Account เดียวกับที่ใช้สร้าง Project Firebase
*   เมื่อสำเร็จ หน้าเว็บจะขึ้น Success และใน Terminal จะขึ้น `Success! Logged in as ...`

### 4.3 เชื่อมต่อ Project
เปลี่ยนเข้าไปในโฟลเดอร์ frontend:
```powershell
cd frontend
```
สั่ง Init:
```powershell
firebase init hosting
```
**การตอบคำถาม (สำคัญมาก ตอบตามนี้):**
1.  **Are you ready to proceed?** -> `y`
2.  **Please select an option:** -> เลื่อนลูกศรลงมาที่ `Use an existing project` แล้วกด Enter (เพราะเรามีโปรเจกต์ใน Firebase แล้วจากไฟล์ที่เห็น)
3.  **Select a default Firebase project:** -> เลือกโปรเจกต์ของเรา (น่าจะชื่อ `nongmax-web` หรือ `xmax-001`)
4.  **What do you want to use as your public directory?** -> พิมพ์ว่า `dist` แล้ว Enter (เพราะ Vite สร้างไฟล์ไว้ที่นี่)
5.  **Configure as a single-page app?** -> `y` (สำคัญ! เพื่อป้องกันปัญหา User Refresh แล้วเจอ 404)
6.  **Set up automatic builds and deploys with GitHub?** -> `n` (เอาไว้ก่อน)
7.  **File dist/index.html already exists. Overwrite?** -> `n` (ห้ามเขียนทับ ถ้าเผลอตอบ y ต้อง build ใหม่)

### 4.4 เชื่อมต่อกับ Backend
เราต้องบอก Frontend ว่า Backend อยู่ที่ไหน โดยการแก้ไฟล์ `.env.production`
1.  สร้างหรือเปิดไฟล์ `frontend\.env.production`
2.  แก้ไขเนื้อหาดังนี้ (เปลี่ยน URL เป็นของ Render ที่ได้จากส่วนที่ 3):
    ```env
    VITE_API_URL=https://nongmim-backend.onrender.com/api
    VITE_SOCKET_URL=https://nongmim-backend.onrender.com
    ```
3.  บันทึกไฟล์

### 4.5 Build และ Deploy
ตอนนี้เราพร้อมส่งขึ้น Server แล้ว รันคำสั่งทีละบรรทัด:

1.  **สร้างไฟล์สำหรับ Production:**
    ```powershell
    npm run build
    ```
    *รอจนเสร็จ จะต้องไม่ขึ้น Error สีแดง*

2.  **ส่งขึ้น Firebase:**
    ```powershell
    firebase deploy --only hosting
    ```

ถ้าสำเร็จ Terminal จะแสดง:
> `Hosting URL: https://nongmax-web.web.app` (ตัวอย่าง)

---

## ส่วนที่ 5: การทดสอบและแก้ไขปัญหา

### 5.1 ขั้นตอนการทดสอบ
1.  เปิด Hosting URL ที่ได้จาก Firebase บน Browser
2.  ลอง **Login** (ถ้ามีระบบ Login) -> ถ้าทำได้ แปลว่าต่อ DB ติด และ Backend รับ Request ได้
3.  ลอง **Chat** หรือฟีเจอร์ Realtime -> ถ้าข้อความขึ้นทันที แปลว่า Socket.io ทำงานถูกต้อง
4.  ลอง **Refresh หน้าเว็บ** ในหน้าย่อยๆ -> ต้องไม่ขึ้น 404 Not Found

### 5.2 ปัญหาที่พบบ่อย (Troubleshooting)

**ปัญหา 1: หน้าเว็บขาวโพลน หรือกด Login แล้วเงียบ**
*   **สาเหตุ:** มักเกิดจาก `VITE_API_URL` ผิด หรือลืม `npm run build` ก่อน deploy
*   **วิธีแก้:** กด F12 บน Chrome ดูแท็บ *Console* ถ้าเห็นสีแดงๆ เกี่ยวกับ CORS หรือ Network Error ให้เช็ค URL Backend ใน `.env.production` แล้ว Build ใหม่ (`npm run build`) -> Deploy ใหม่

**ปัญหา 2: Render ขึ้น Build Failed**
*   **สาเหตุ:** มักเกิดจากโครงสร้างโฟลเดอร์ผิด หรือลืมตั้ง `Root Directory` เป็น `Backend`
*   **วิธีแก้:** ไปที่ Render > Settings และเช็ค Root Directory ต้องเป็น `Backend`

**ปัญหา 3: เชื่อมต่อฐานข้อมูลไม่ได้ (Database Connection Error)**
*   **สาเหตุ:** ใส่ค่า DB_HOST ผิด หรือ Database ยังไม่ได้เปิด Public Access
*   **วิธีแก้:** เช็คค่า Environment Variables ใน Render ให้แน่ใจว่า Username/Password ถูกต้อง และ Host ไม่มี `http://` นำหน้า

**ปัญหา 4: CORS Error (Access to fetch ... has been blocked)**
*   **สาเหตุ:** Backend ไม่อนุญาตให้ Frontend URL นี้เข้ามาดึงข้อมูล
*   **วิธีแก้:** กลับไปที่ไฟล์ `Backend/server.js` ตรง `allowedOrigins` ให้เพิ่ม URL ของ Firebase (เช่น `https://nongmax-web.web.app`) เข้าไป แล้ว Push โค้ดขึ้น GitHub เพื่อให้ Render อัปเดตใหม่

